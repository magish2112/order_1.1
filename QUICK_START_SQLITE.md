# 🚀 БЫСТРЫЙ СТАРТ С SQLITE (Без Docker)

## Проблема
PostgreSQL не запущен. API не может подключиться к базе данных.

## ✅ Решение: Использовать SQLite

SQLite - это база данных в виде файла, не требует установки сервера.

---

## 📋 ШАГ ЗА ШАГОМ

### 1. Остановить текущий API сервер
В терминале где запущен API нажмите `Ctrl+C`

### 2. Переименовать файл окружения

```bash
cd apps/api

# Сохраним текущий .env
move .env .env.postgres.backup

# Используем SQLite конфигурацию
copy env.sqlite .env
```

### 3. Обновить схему Prisma для SQLite

Откройте файл `apps/api/prisma/schema.prisma` и измените:

```prisma
datasource db {
  provider = "sqlite"        // Было: "postgresql"
  url      = env("DATABASE_URL")
}
```

### 4. Создать базу данных SQLite

```bash
cd apps/api

# Создать и применить миграции
npx prisma migrate dev --name init

# Заполнить базу данными
npm run prisma:seed
```

### 5. Запустить API

```bash
cd apps/api
npm run dev
```

---

## 🎯 Теперь должно работать!

- ✅ API запустится и создаст файл `dev.db` в папке `apps/api/`
- ✅ Все данные будут храниться в этом файле
- ✅ Не нужен PostgreSQL или Docker

---

## 📝 Важные замечания

### SQLite vs PostgreSQL

**SQLite - подходит для:**
- ✅ Разработки
- ✅ Тестирования
- ✅ Небольших проектов
- ✅ Когда не хочется настраивать Docker

**PostgreSQL - нужен для:**
- ⚠️ Production
- ⚠️ Высокой нагрузки
- ⚠️ Когда нужны advanced возможности БД

### Если хотите вернуться на PostgreSQL

```bash
cd apps/api

# Вернуть старый .env
move .env.postgres.backup .env

# Запустить Docker
docker-compose up -d postgres redis minio

# Применить миграции
npx prisma migrate dev

# Заполнить данными
npm run prisma:seed
```

---

## ✅ ГОТОВО!

Теперь проект должен работать без Docker и PostgreSQL!
