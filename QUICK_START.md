# 🚀 БЫСТРЫЙ СТАРТ - SQLite версия

## ⚡ Запуск за 3 минуты

### Шаг 1: Установка зависимостей

```bash
# API
cd apps/api
npm install

# Admin Panel
cd ../admin
npm install

# Frontend
cd ../web
npm install
```

### Шаг 2: Настройка базы данных SQLite

**Windows:**
```bash
cd apps/api
setup-sqlite.bat
```

**Linux/Mac:**
```bash
cd apps/api
chmod +x setup-sqlite.sh
./setup-sqlite.sh
```

Это автоматически:
- ✅ Создаст базу данных `dev.db`
- ✅ Применит все миграции
- ✅ Заполнит базу начальными данными
- ✅ Создаст администратора

### Шаг 3: Запустить приложения

Откройте **3 терминала**:

**Терминал 1 - API:**
```bash
cd apps/api
npm run dev
```
→ http://localhost:4000

**Терминал 2 - Admin Panel:**
```bash
cd apps/admin
npm run dev
```
→ http://localhost:3001

**Терминал 3 - Frontend:**
```bash
cd apps/web
npm run dev
```
→ http://localhost:3000

---

## 🎯 ГОТОВО!

### Доступ к админ-панели:
- **URL:** http://localhost:3001
- **Email:** admin@example.com
- **Password:** admin123

### API документация:
- **URL:** http://localhost:4000/api/docs

---

## 📁 Структура проекта

```
order_1.1/
├── apps/
│   ├── api/                 # Backend (Fastify + SQLite)
│   │   ├── dev.db          # База данных (создается автоматически)
│   │   ├── uploads/        # Загруженные файлы
│   │   └── prisma/
│   │       └── migrations/ # Миграции SQLite
│   ├── admin/              # Админ-панель (React + Vite)
│   └── web/                # Публичный сайт (Next.js)
├── setup-sqlite.bat        # Автоустановка (Windows)
└── setup-sqlite.sh         # Автоустановка (Linux/Mac)
```

---

## 🔧 Полезные команды

### База данных

```bash
cd apps/api

# Просмотр данных (GUI)
npx prisma studio

# Создать новую миграцию
npx prisma migrate dev --name имя_миграции

# Пересоздать базу (удалит все данные!)
rm dev.db
npx prisma migrate dev
npm run prisma:seed
```

### Админ

```bash
# Создать нового администратора
cd apps/api
node create-admin.js
```

---

## 🐛 Решение проблем

### База данных не создается

```bash
cd apps/api
rm -rf prisma/migrations
rm dev.db
npx prisma migrate dev --name init
npm run prisma:seed
```

### Ошибка "database is locked"

```bash
# Остановить все процессы
# Ctrl+C во всех терминалах
# Запустить заново
```

### Порт занят

Измените порты в `.env` файлах:
```env
# apps/api/.env
PORT=4001

# Или используйте другие порты
```

---

## 📚 Дополнительно

- **Полное руководство:** `SQLITE_MIGRATION_GUIDE.md`
- **Настройка production:** `ENV_EXAMPLE.md`
- **Отчет по исправлениям:** `FINAL_REPORT.md`

---

**Всё работает? Отлично! 🎉**

**Не работает?** Читайте `SQLITE_MIGRATION_GUIDE.md`
