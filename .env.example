# Environment переменные для проекта Eterno

# ===========================================
# ВАЖНО: Скопируйте этот файл в .env
# и заполните все значения перед запуском!
# ===========================================

# Node Environment
NODE_ENV=production

# ===========================================
# PostgreSQL Database
# ===========================================
POSTGRES_DB=eterno_db
POSTGRES_USER=eterno_user
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD_HERE

# Database connection string (auto-generated in Docker)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# ===========================================
# Redis Cache
# ===========================================
REDIS_URL=redis://redis:6379

# ===========================================
# JWT Secrets (минимум 32 символа каждый)
# Генерация: openssl rand -base64 32
# ===========================================
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_32_CHARS_OR_MORE_HERE
JWT_REFRESH_SECRET=CHANGE_ME_DIFFERENT_RANDOM_32_CHARS_OR_MORE_HERE
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ===========================================
# CORS Settings
# ===========================================
# Development: * (все разрешены)
# Production: https://yourdomain.com,https://admin.yourdomain.com
CORS_ORIGIN=*

# ===========================================
# Public URLs (для production)
# ===========================================
# URL для API (используется на frontend)
NEXT_PUBLIC_API_URL=http://localhost

# URL для админки (используется в email уведомлениях)
ADMIN_URL=http://localhost:3001

# URL для Swagger документации
SWAGGER_HOST=localhost
SWAGGER_SCHEME=http

# ===========================================
# SMTP Email Settings (опционально)
# ===========================================
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@yourdomain.com

# ===========================================
# File Uploads
# ===========================================
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/app/public/uploads
PUBLIC_UPLOAD_URL=/uploads

# ===========================================
# Ports (можно оставить по умолчанию)
# ===========================================
API_PORT=4000
WEB_PORT=3000
ADMIN_PORT=3001
NGINX_PORT=80
POSTGRES_PORT=5432
REDIS_PORT=6379

# ===========================================
# Vite (для Admin панели)
# ===========================================
VITE_API_URL=http://localhost/api/v1

# ===========================================
# Development (optional)
# ===========================================
DEV_ADMIN_EMAIL=admin@example.com
DEV_ADMIN_PASSWORD=admin123
